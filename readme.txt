To compile these files, corresponding sdk support(Kinect sdk(KinectSDK-v2.0_1409) or RealSence sdk(intel_rs_sdk_offline_4.0.0.112526)
is needed.some results can be found in the Source.zip(No code);
Usage of each function can be find at corresponding *.h (interfaces has thier comments), 
if test with realsense's sdk, run RealSense with rsMainFrame() defined in RealSensePxc.cpp
$Opencv is required$
